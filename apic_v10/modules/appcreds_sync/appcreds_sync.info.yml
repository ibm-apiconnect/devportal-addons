name: IBM APIC Portal - Synchronise Application Credentials
description: Push application credentials (client ids and secrets) to an external server
package: Custom
core_version_requirement: ^8 || ^9 || ^10 || ^11
type: module
version: 1.0.1
dependencies:
  - apic_app
